# Tabox Chrome Web Store — Feature Screenshots

Four 1280×800 screenshots extend the approved Night Flow brand direction with distinct ImageGen campaign illustrations. The generated art supplies the feature metaphor and depth; the canonical logo, Comfortaa typography, exact copy, Pro badges, and pricing are composited deterministically in `screenshots.html`. The real Tabox extension UI remains reserved for the first store screenshot.

- `02-sync-across-devices.png`
- `03-find-any-tab.png`
- `04-share-folders-collections-pro.png`
- `05-tabox-ai-pro.png`

All marketing text uses Comfortaa. The `Tabox` wordmark is solid white, the slogan is purple → blue → orange, and the Pro screens use the required solid `Tabox` + gradient `PRO` badge with annual-billing price copy. The Pro graphics are deliberately distinct: human collaboration for sharing, abstract tab transformation for AI.

Selected built-in ImageGen source assets are stored in `generated/` with versioned filenames.

Open `screenshots.html?shot=sync`, `search`, `share`, or `ai` to edit and render an individual artboard.
